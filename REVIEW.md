# 代码审查记录

本轮检查了策略构建、交易入口、RFQ 状态管理、配置校验和前端轮询，完成以下修复。

| 问题 | 修复 |
| --- | --- |
| RFQ 执行绕过全局实盘开关 | 复用 `can_trade_live`，并保留显式确认要求 |
| 行情刷新失败后仍能使用过期快照下单 | 普通实盘开仓、RFQ 创建及执行检查快照来源和时间 |
| 开仓只检查保证金，最大亏损可能超过配置限制 | 同时检查最大亏损和预估保证金；RFQ 执行按实际四腿报价检查最大亏损 |
| RFQ 并发操作、重复执行或覆盖活动询价 | 串行处理状态变更；提交前持久化执行意图；拒绝再次执行结果不明的请求 |
| RFQ 未验证完整腿集合、数量和报价有效期 | 后端校验四腿合约、方向、数量、价格、行权价顺序及有效期 |
| 模拟平仓可能清除实盘跟踪状态 | 模拟平仓只处理模拟持仓，保留实盘跟踪信息 |
| 非有限数量及缺少行情的合约进入计算 | 拒绝非法数量和配置，过滤不可用价格与 Delta，明确报告缺腿及交叉行权价 |
| 前端将部分失败订单提示为已提交 | 按各腿状态显示结果，保留执行中按钮状态 |
| 行情请求进行中修改数量会丢失刷新 | 合并为一次使用最新数量的后续请求 |

## 订单对账和收益匹配的后续修复

此前记录的三项剩余问题已完成代码修复：

- **BBO 异常与撤单确认**：独立的 `OrderExecutor` 处理限价跟单、异常清理、任务取消和终态确认。按合约与 `orderLinkId` 查询实时订单，缺失时查询历史订单；只使用校验后的累计成交量。未确认终态时保留 `unknown`，不会返回已撤单或自动补单。
- **市价补单**：移除按账户仓位推断缺腿的逻辑。重新确认原订单终态后，以目标数量减去原订单累计成交量计算补单量；校验数量下限及步长。补单响应不明时不再次发送。原单和补单成交记录共同计入结果及手续费。
- **收益匹配**：成交按时间先后匹配并消耗开仓数量，分批成交按数量分摊手续费；按成交 ID 去重，按币种及已知开仓组合隔离，不使用未来开仓成交填补缺失历史。记录不足时显示待匹配。

同时保存订单请求和已确认成交增量，防止重复扣减持仓；重启后继续核对未决订单。已有策略或未决订单会阻止新策略覆盖跟踪信息。新订单采用累计成交量跟踪，不再用旧的四腿恢复逻辑回填请求数量。订单状态未变化时不重复写入状态文件，各腿恢复对账并发进行。

实现依据：Bybit 说明撤单返回仅代表接受请求，最终状态需要另行确认；实时订单接口提供累计成交量，部分历史终态需转查订单历史。参见 [撤单文档](https://bybit-exchange.github.io/docs/v5/order/cancel-order) 和 [实时订单文档](https://bybit-exchange.github.io/docs/v5/order/open-order)。本项目使用有限次数的 REST 查询对账；交易所未返回可确认终态时保守保留未决状态。

## RFQ 恢复、状态文件与持续风控

- RFQ 首次应用持仓后保存 `tracking_applied`，重复刷新、重启和旧状态迁移均保留部分平仓后的数量；检测已有 RFQ 跟踪或平仓记录，不重新创建已平仓持仓。
- 新增状态格式版本与结构校验。读取失败不部分加载状态，写入失败保留旧文件；交易入口和后台状态更新拒绝继续使用损坏状态，健康接口及页面报告阻止原因。写入采用独立临时文件、刷盘后原子替换。
- 每次开仓下单和改单前，累积各腿曾保留的最不利限价，重新检查完整组合到期最大亏损。不会因价格改善立即释放旧价格的预算，以覆盖改单与成交竞态。
- 原 `ALLOW_MARKET_FALLBACK` 配置现在触发带价格限制的 IOC 限价补单，买入使用最高买价、卖出使用最低卖价，并经过相同风险检查；不发送无应用层价格边界的市价补单。参数依据 [Bybit 下单文档](https://bybit-exchange.github.io/docs/v5/order/create-order)。

## 验证与边界

- 后端：70 项单元及接口测试通过。
- 前端：9 项 Node.js 行为测试通过。
- JavaScript 语法检查和 `git diff --check` 通过。
- 覆盖下单响应丢失、改单失败、撤单成交竞态、终态查询失败、任务取消、重启恢复、部分成交补单、补单响应丢失、数量下限、部分平仓和分批成交收益。
- 所有交易测试使用模拟响应，未使用真实账户或发送交易请求。
- 未进行交易所实盘端到端验证。断网或进程被强制终止时无法保证立即撤单，持久化日志用于恢复后继续对账；交易所始终查询不到的订单需要人工核对。
- 页面保留最近 100 条成交，不是完整账户账本。旧状态文件缺少新订单日志时沿用已有恢复行为；本次没有伪造历史订单的终态。

持续风控检查针对四腿完整成交后的到期最大亏损，不含手续费；四腿并非原子成交，中间敞口仍需核对。历史成交查询的完整分页、多进程共享状态及实盘端到端验证尚未完成。
